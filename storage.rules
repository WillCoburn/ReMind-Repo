rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/exports/{document=**} {
      allow write: if request.auth != null && request.auth.uid == userId;
      allow read: if false; // reads go through signed URLs or token links
    }
  }
}

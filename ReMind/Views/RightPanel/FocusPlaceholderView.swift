import SwiftUI

struct FocusPlaceholderView: View {
    var body: some View {
        RightPanelPlaceholderDetailView(
            title: "Focus",
            subtitle: "Stay on track",
            icon: "target",
            tint: .orange
        )
    }
}

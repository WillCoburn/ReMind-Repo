import SwiftUI

struct EnergyPlaceholderView: View {
    var body: some View {
        RightPanelPlaceholderDetailView(
            title: "Energy",
            subtitle: "Check balance",
            icon: "bolt.fill",
            tint: .teal
        )
    }
}

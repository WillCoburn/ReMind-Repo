import SwiftUI

struct NotesPlaceholderView: View {
    var body: some View {
        RightPanelPlaceholderDetailView(
            title: "Notes",
            subtitle: "Jot ideas",
            icon: "square.and.pencil",
            tint: .purple
        )
    }
}

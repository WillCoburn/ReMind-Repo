import SwiftUI

struct MoodPlaceholderView: View {
    var body: some View {
        RightPanelPlaceholderDetailView(
            title: "Mood",
            subtitle: "Track feelings",
            icon: "face.smiling",
            tint: .mint
        )
    }
}

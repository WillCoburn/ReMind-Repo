import SwiftUI

struct ActivityPlaceholderView: View {
    var body: some View {
        RightPanelPlaceholderDetailView(
            title: "Activity",
            subtitle: "Move more",
            icon: "figure.walk",
            tint: .green
        )
    }
}

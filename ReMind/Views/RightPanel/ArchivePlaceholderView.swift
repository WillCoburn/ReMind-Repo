import SwiftUI

struct ArchivePlaceholderView: View {
    var body: some View {
        RightPanelPlaceholderDetailView(
            title: "Archive",
            subtitle: "Keep records",
            icon: "tray.full.fill",
            tint: .pink
        )
    }
}

import SwiftUI

struct HabitsPlaceholderView: View {
    var body: some View {
        RightPanelPlaceholderDetailView(
            title: "Habits",
            subtitle: "Daily steps",
            icon: "flame.fill",
            tint: .red
        )
    }
}
